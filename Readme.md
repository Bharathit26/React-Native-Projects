# To connect to MongoDB install mongoDB from MongoDBCommunity and install mongodbservercommunity and mongosh shell.

# Also while installing the mongodb application click the checkbox which says to install MongoDB compass which is GUI tool, which shows the databases.
